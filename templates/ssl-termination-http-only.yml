http:
  routers:
    {{NAME}}-http:
      rule: {{RULE}}
      service: {{NAME}}-service
      entryPoints: [web]
  services:
    {{NAME}}-service:
      loadBalancer:
        servers:
          - url: http://{{IP}}:{{PORT}}
